server:
  port: 8081
spring:
  application:
    name: OrderService
  datasource:
    url: jdbc:postgresql://localhost:15432/order_service
    username: postgres
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true